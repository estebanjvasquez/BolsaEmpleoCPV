# Supabase Supavisor transaction pooler (port 6543) — runtime queries via Hyperdrive
DATABASE_URL="postgresql://postgres:[password]@[project-ref].pooler.supabase.com:6543/postgres"

# Supabase direct/session connection (port 5432) — used only by `prisma migrate`
DIRECT_URL="postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres"
